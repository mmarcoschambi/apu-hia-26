name: combo_pullback_entry
status: GO
pbo: 0.18  # Probability of overfitting (bajo = bueno)
wf_sharpe_mean: 1.36
wf_sharpe_min: 1.3
wf_sortino_mean: 1.85
wf_max_drawdown: 0.12
scanner_filter: pullback
pattern_filter: hammer,doji,pinbar
regime_blocked:
- 3
- 4
fee_rate: 0.001
slippage_rate: 0.001
spx_sma_period: 50
vix_max: 25.0
spx_vol_max: 20.0
green_light_points: 5
lookback_days: 180
max_setups: 5
flat_base_range_pct: 0.15
vcp_contraction_threshold: 0.7
min_rvol: 1.0
min_adr: 1.0
min_consolidation_days: 5
max_positions: 4
max_position_pct: 0.25
max_exposure_pct: 0.65
alerts:
- PBO=18% — robusto
- Sharpe WF medio=1.36 sobre mínimo=1.30 — margen estrecho
notes: 'Combo conservador enfocado en pullbacks. Validado con walk-forward 6 ventanas,
  3 meses cada una. OOS Sharpe=1.28, dentro de tolerancia.

  '
